const NO_RIDES_MESSAGE = 'Could not find any rides';
const UNKNOWN_ERROR = 'Unknown error';

const START_COORDINATES_ERROR =
  'Start latitude and longitude must be between -90 - 90 and -180 to 180 degrees respectively';
const END_COORDINATES_ERROR =
  'End latitude and longitude must be between -90 - 90 and -180 to 180 degrees respectively';
const RIDER_ERROR = 'Rider name must be a non empty string';

module.exports = {
  NO_RIDES_MESSAGE,
  UNKNOWN_ERROR,
  START_COORDINATES_ERROR,
  END_COORDINATES_ERROR,
  RIDER_ERROR,
};
